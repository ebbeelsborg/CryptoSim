{
  "name": "rool-crypto-sim",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd crypto-sim && npm run dev",
    "build": "cd backend && npm run build && cd ../crypto-sim && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../crypto-sim && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
